node_modules/
node_modules/
.next/
out/
.env
.env.local
.env*.local
.env.production
*.log
.DS_Store
dist/
coverage/
.vscode/
.idea/
gcs-key.json

# Screenshot / tooling artifacts (keep public/ assets, ignore loose root pngs)
/*.png
.playwright-mcp/
tsconfig.tsbuildinfo

# Local Claude Code config (per-machine skills/state)
.claude/

# Runtime-generated local uploads (keep folder marker only)
/public/uploads/*
!/public/uploads/.gitkeep

# Local Gemini expansion cache (regenerates as games are played)
/cache/

# VM deploy .env template — secret içerebilir, repo'ya girmemeli.
.env.vm.txt
.env.vm

# BMad agent intermediate output (kişisel/local)
_bmad-output/
_bmad/custom/*.user.toml
